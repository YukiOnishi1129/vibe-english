{
  "name": "@vibe-english/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@vibe-english/db": "*",
    "@vibe-english/domain": "*",
    "better-auth": "^1.7.5",
    "hono": "^4.13.8",
    "kysely": "^0.28.17"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "wrangler": "^4.30.0"
  }
}
